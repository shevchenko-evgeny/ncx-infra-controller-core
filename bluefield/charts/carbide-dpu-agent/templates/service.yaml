apiVersion: v1
kind: Service
metadata:
  name: {{ include "carbide-dpu-agent.fullname" . }}
  labels:
    {{- include "carbide-dpu-agent.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  internalTrafficPolicy: Local
  selector:
    {{- include "carbide-dpu-agent.selectorLabels" . | nindent 4 }}
    {{- with .Values.serviceDaemonSet.labels }}
      {{- toYaml . | nindent 4 }}
    {{- end }}
  ports:
    - name: metrics
      protocol: TCP
      port: {{ include "carbide-dpu-agent.metricsPort" . | int }}
      targetPort: {{ include "carbide-dpu-agent.metricsPort" . | int }}

